<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open Excel/spreadsheet files from WhatsApp / Files / email. Content
                 URIs from a sharing app (WhatsApp's own FileProvider, Gmail, a
                 file manager…) need an explicit scheme declared alongside the
                 mimeType — a mimeType-only <data> element is not reliably
                 matched against content:// (or file://) URIs on every Android
                 version/OEM skin, which silently drops this app out of the
                 "Open with" chooser entirely. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" /> <!-- .xlsx -->
                <data android:mimeType="application/vnd.ms-excel" /> <!-- .xls -->
                <data android:mimeType="application/vnd.ms-excel.sheet.binary.macroenabled.12" /> <!-- .xlsb -->
                <data android:mimeType="application/vnd.ms-excel.sheet.macroenabled.12" /> <!-- .xlsm -->
                <data android:mimeType="application/vnd.oasis.opendocument.spreadsheet" /> <!-- .ods -->
            </intent-filter>

            <!-- الصيغ اللي أندرويد نفسه مش عارفها بتوصل من واتساب بنوع عام
                 (application/octet-stream) — لأن جدول الأنواع في أندرويد
                 مافيهوش .xlsb أصلاً، فتليفون اللي باعت الملف بيسمّيه «ملف
                 مجهول». من غير السطر ده، محفظة .xlsb مابتوريش البرنامج في
                 قايمة «فتح بواسطة» رغم إن النوع الرسمي معلَن فوق.

                 فلتر منفصل من غير BROWSABLE بقصد: كده البرنامج مايتعرضش
                 على كل تنزيل مجهول من المتصفح، بس يفضل ظاهر للملفات اللي
                 المندوب بيبعتها من واتساب أو من مدير الملفات. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- منظّم الإيقاع: الاهتزاز أثناء التسجيل. محتاج بناء APK جديد عشان يتفعّل. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
