"use client";

/** صفحة سجلات المجموعة — الغلاف بس؛ المنطق كله في المكوّن المشترك. */
import GroupRecordsView from "@/components/GroupRecordsView";

export default function GroupRecordsPage() {
  return <GroupRecordsView />;
}
