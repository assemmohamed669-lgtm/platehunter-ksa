# 🧭 HANDOFF — إحنا فين دلوقتي (VoiceX على قناص اللوحات)

> ملف تسليم لأي جلسة/أكونت جديد يكمّل من هنا. آخر تحديث: ٤ سبتمبر ٢٠٢٦.
> **الذاكرة الكاملة** في `~/.claude/projects/C--Users-assem-...-platehunter-ksa/memory/` (محلية على الجهاز، مش مربوطة بالأكونت) — اقرا `MEMORY.md` هناك، خصوصاً `project_voicex_server_migration.md` (السيرفر الحالي) و`project_voicex_app_integration.md` و`project_voice_training_errors_log.md`.

---

## المشروع باختصار
**VoiceX** = موديل صوت مخصّص (Whisper مدرَّب على لوحات السعودية) اندمج في تطبيق **قناص اللوحات** (`platehunter-ksa`, Next.js/Vercel) في صفحة التشييك (`app/(app)/instant-check/page.tsx`) كخيار صوتي بديل لديبجرام. المندوب يقول اللوحات بصوته → تتفرّغ لحظياً → تتطابق على شيت التشييك.

## المعمارية (محدَّثة ٣ سبتمبر — الموديل **ساب اللابتوب**)
- **الموديل بيشتغل على سيرفر مؤجّر: Vast.ai · RTX 3090 · كوريا الجنوبية** — `instance 49755144` · IP `125.190.184.215` · **SSH بورت 30986** · مفتاح `~/.ssh/voicex_vast_key`.
- **$0.155/ساعة ≈ $112/شهر · فوترة بالثانية من رصيد مسبق** (مش اشتراك). لو الرصيد خلص → السيرفر يقف والمناديب يرجعوا لديبجرام صامت.
- **الوصول** عبر نفق Cloudflare **من السيرفر نفسه** (مش من اللابتوب). ⚠️ **HTTPS إجباري** — `normalizeJudgeBase` بترفض http إلا للـlocalhost والمتصفح بيحجب mixed content، فـIP:port المباشر **مش هينفع**.
- **حارسان بـauto-restart على السيرفر:** `run_seg_server.sh` (بيسِت `SEG_LOG_DIR`/`PLATE_JUDGE_TOKEN` صراحةً — جلسة SSH مابترثش متغيّرات `docker -e`) و`run_tunnel.sh` (بينشر اللينك الجديد تلقائياً على `/api/voicex/tunnel`).
- `seg_server.py` على السيرفر **معدَّل يسمع على 0.0.0.0** (الأصل 127.0.0.1) + `--max-inflight 40` (الافتراضي 2 كان هيفشّل عند ٣ مناديب متزامنين).
- **ديبجرام** = احتياطي صامت لو VoiceX فصل.

## ✅ مشكلة النفق **اتحلّت** (كانت أكبر مشكلة مفتوحة)
`VOICEX_TUNNEL_SECRET` اتسِت على Vercel وفي `run_tunnel.sh` (نفس القيمة) — **النشر التلقائي متحقّق منه فعلياً**. لو النفق أعاد تشغيل نفسه، اللينك الجديد بينزل Supabase لوحده بلا تدخّل.

## التشغيل اليومي (تلقائي، مش محتاج تدخّل)
مهمة ويندوز **«VoiceX Harvest Daily Pull» ١٠ صباحاً** (`StartWhenAvailable` فمابتفوتش لو الجهاز كان مقفول) → `D:\voicex-harvest\pull_voicex_harvest.ps1`:
سحب الحصاد → توليد **`report.md`** (أزواج الترفرف بأسماء الملفات + نسبة الرفض لكل مندوب) → **مسح من السيرفر بعد التأكد من تطابق العدد** (مايمسحش لو السحب ناقص).

**قياس الأداء (٣ سبتمبر، ٣٦٢ مقطع حقيقي):** ١٠ متزامن ٩٩.٧٪ · ٢٠ متزامن ٩٩.٥٪ · ٣٠ متزامن ٩٨.٤٪. زمن الرد ~٩٧٧ملي **منها ٨٥-١٤٠ملي استدلال بس** — الباقي شبكة (المسافة لكوريا). مافيش سيرفر في الخليج بسعرنا (المتاح H100/H200 بـ$2000+/شهر).

## الحصاد (داتا التدريب)
- بيتسجّل على **`D:/voicex-harvest/live-voicex-*`** (نقلناه من C عشان مايملاش قرص النظام)، بسقف مساحة.
- **~٣٦,٦٤١ مقطع، ~٣٠,٦٧٥ نافذة لوحة**، من ~٨ مناديب، ظروف حقيقية (قيادة/ضوضاء). التسميات pseudo-labels محتاجة تنضيف.
- الموسوم باسم المندوب بيشتغل من ٣ سبتمبر (ترويسة X-Agent-Id).

## الموديل الحالي
- آخر تدريب: **٢ سبتمبر ٩:٥٤**، على **~٤٨,٠٠٠ مقطع** (batch 8, epoch واحد, اتحفظ عند step 4000 = 0.66 epoch).
- 🔴 **بروفايل الخطأ اتغيّر (قياس ٣ سبتمبر):** بقى **الأرقام** مش الحرف الأول — ٣٦ خطأ رقم مقابل خطأ onset **واحد** في جلسة ٣٦٢ مقطع. تعديل «قراءة النطق الكامل» حلّ قص الحرف الأول. **أولوية التدريب الجاي = تمييز الأرقام (آخر خانة).**
- **الهلوسة متصلّحة (begin_suppress) — حافظ عليها.** ⚠️ الهلوسة كانت **ثابتة** (نفس اللوحة على السكوت) فبتعدّي الإجماع — الحل كان ١٢٠٠ سلبي غير-كلامي.
- سجل الأخطاء الكامل للتدريب الجاي: `plate-voice-lab/TRAINING_ERRORS_TO_AVOID.md`.

## اللي اتعمل (PRs مدموجة على main، الأحدث فوق)
- **#150** تبويب «فرز» للمشترك صوت-فقط (إحالة + لصق على سجلاته) — ⚠️ ماتجربش على جهاز لسه.
- **#153** زر واحد يفتح/يقفل الصوت لكل المناديب (سوبر أدمن، المناديب بس).
- **#152** مسح السجلات بيوصل السيرفر (كانت بترجع بعد المسح).
- **#151** حارس التوأم يمسك «الاتنين مفردين» من نفس النطق — `lib/twinGuard.ts` + ٢١ اختبار فيهم مجموعة «أمان» تثبت إن لوحتين حقيقيتين مايتلمسوش.
- #148 حارس التوأم يمسك ترفرف الرقم (بنح6706/6702) — آمن (مايرميش مؤكّدة).
- #147 قراءة النطق الكامل (تحل ترفرف الكلام البطيء).
- #146 استجابة فورية (جسّ /health أول الجلسة → ديبجرام فوراً لو النفق واقع) + حارس توأم للترفرف الحرفي.
- #145 إزالة إعادة قراءة آخر نافذة (كانت بتعمل تكرار).
- #144 شفاء ذاتي (يرجّع VoiceX لما النفق يرجع).
- #143 نسخ الكل بالتفاصيل + إصلاح الحي.
- #141 مشاركة كل لوحات الصوت على واتساب (نص مختصر + نسخ الكل).
- #139 ألوان أزرار الصلاحيات (أخضر مفتوح/أحمر مقفول).
- #138 وسم الحصاد باسم المندوب (X-Agent-Id).
- #137 الأدمن يتحكّم في صوت المناديب. #136 زر مشاركة إكسيل.
- #132–#134 محرك المعمل بالحرف + مؤشّر صوت + آخر لوحة ماتضيعش.

## ✅ السيرفر المؤجّر — **اتنفّذ** (كان قرار معلّق)
اتاخد **Vast.ai RTX 3090 كوريا** بدل RunPod: RunPod كان **$540/شهر** لنفس الكارت وقت الفحص (والأرخص عنده نافد المخزون)، وVast.ai بـ**$112/شهر** بمضيف موثّق ٩٩.٦٪. مافيش سيرفر في الخليج بسعرنا.
**⚠️ الفوترة بالثانية من رصيد مسبق** — لازم متابعة الرصيد، مفيش تجديد تلقائي إلا لو المالك فعّله من Billing.

## قواعد شغل مهمة (من الذاكرة)
- **ترفرف اللوحة:** المالك رفض الدمج بالثقة (خوف يسيب الغلط). الحل المقبول = حارس توأم بـ**اتفاق النوافذ** (يشيل المفرد بس لو ليه توأم مؤكّد نافذتين+، عمره ما يرمي مؤكّدة → الأسطول الحقيقي يفضل).
- **الحل الجذري للترفرف/الـonset = تدريب** على الحصاد (مش كود).
- كل fix فرع نضيف من origin/main (الريبو squash-merge فبيباعد).
- عربي أول في الردود.

## الوضع الحالي والخطوة الجاية (٤ سبتمبر)
**المالك فتح تجربة مجانية ٣ أيام لكل المناديب** عشان يجرّبوا الصوت ويتجمع حصاد من أصوات مختلفة.
**القرار: مافيش تعديلات كود على المحرك دلوقتي — نستنى الداتا تتجمع الأول.**

الخطوة الجاية بعد التجربة:
1. مراجعة الحصاد + التقارير اليومية (`report.md` لكل يوم).
2. تدريب جديد على **معايير القبول الستة** المسجّلة في `plate-voice-lab/TRAINING_PLAN_NEXT.md` قسم ٠ (السرعة/التلزيق · التردّد والتصحيح · صفر هلوسة · سرعة الظهور · الأرقام المركّبة · نوع السيارة).
3. `lib/vehicleType.ts` جاهز ومقفول على قائمة — الناقص ربطه بمسار VoiceX + إضافة «حادث».
