# 🧭 HANDOFF — إحنا فين دلوقتي (VoiceX على قناص اللوحات)

> ملف تسليم لأي جلسة/أكونت جديد يكمّل من هنا. آخر تحديث: ٤ سبتمبر ٢٠٢٦.
> **الذاكرة الكاملة** في `~/.claude/projects/C--Users-assem-...-platehunter-ksa/memory/` (محلية على الجهاز، مش مربوطة بالأكونت) — اقرا `MEMORY.md` هناك، خصوصاً `project_voicex_app_integration.md` و`project_voice_training_errors_log.md`.

---

## المشروع باختصار
**VoiceX** = موديل صوت مخصّص (Whisper مدرَّب على لوحات السعودية) اندمج في تطبيق **قناص اللوحات** (`platehunter-ksa`, Next.js/Vercel) في صفحة التشييك (`app/(app)/instant-check/page.tsx`) كخيار صوتي بديل لديبجرام. المندوب يقول اللوحات بصوته → تتفرّغ لحظياً → تتطابق على شيت التشييك.

## المعمارية (مهم)
- **الموديل** بيشتغل على لابتوب المالك: `serving/seg_server.py` (بورت 127.0.0.1:8757) في ريبو **`plate-voice-lab`** (منفصل، فيه git). الموديل نفسه في `C:/Users/assem/Desktop/VoiceX`.
- **الوصول للموبايل** عبر **نفق Cloudflare مجاني** (trycloudflare). التطبيق بيقرا لينك النفق من جدول **`voicex_pointer`** في Supabase (realtime).
- **الحارس** `serving/voicex-guardian.ps1`: بيشغّل السيرفر + النفق + بيراقب الذاكرة ويعيد التشغيل عند التسريب.
- **ديبجرام** = احتياطي صامت لو VoiceX فصل.

## 🔴 أكبر مشكلة مفتوحة (النفق)
**النفق المجاني بيقع ويغيّر لينكه، والحارس مش بينشر المؤشّر تلقائياً** (لأن `VOICEX_TUNNEL_SECRET` مش متسِت). فلما اللينك يتغيّر، المؤشّر يبقى قديم → **الناس مش بتوصل VoiceX → ترجع ديبجرام (زفت)**.
- **الإصلاح المؤقت لما يقع:** جيب اللينك الحالي `curl -s http://127.0.0.1:20242/quicktunnel`، وشغّل في Supabase: `select public.set_voicex_url('https://<اللينك>');`
- **الإصلاح الدائم:** سيرفر مؤجّر بلينك ثابت (القرار جاري — شوف تحت). أو سِت `VOICEX_TUNNEL_SECRET` على Vercel + الحارس عشان ينشر أوتوماتيك.

## الحصاد (داتا التدريب)
- بيتسجّل على **`D:/voicex-harvest/live-voicex-*`** (نقلناه من C عشان مايملاش قرص النظام)، بسقف مساحة.
- **~٣٦,٦٤١ مقطع، ~٣٠,٦٧٥ نافذة لوحة**، من ~٨ مناديب، ظروف حقيقية (قيادة/ضوضاء). التسميات pseudo-labels محتاجة تنضيف.
- الموسوم باسم المندوب بيشتغل من ٣ سبتمبر (ترويسة X-Agent-Id).

## الموديل الحالي
- آخر تدريب: **٢ سبتمبر ٩:٥٤**، على **~٤٨,٠٠٠ مقطع** (batch 8, epoch واحد, اتحفظ عند step 4000 = 0.66 epoch).
- **أكبر مشكلة دقّة = لخبطة الحرف الأول (onset):** د↔ر 174، ح↔ر 159، ب↔ر 98 — الموديل بيميل لـ«ر». + ترفرف + حساسية ضوضاء. **الهلوسة متصلّحة (begin_suppress) — حافظ عليها.**
- سجل الأخطاء الكامل للتدريب الجاي: `plate-voice-lab/TRAINING_ERRORS_TO_AVOID.md`.

## اللي اتعمل (PRs مدموجة على main، الأحدث فوق)
- #148 حارس التوأم يمسك ترفرف الرقم (بنح6706/6702) — آمن (مايرميش مؤكّدة).
- #147 قراءة النطق الكامل (تحل ترفرف الكلام البطيء).
- #146 استجابة فورية (جسّ /health أول الجلسة → ديبجرام فوراً لو النفق واقع) + حارس توأم للترفرف الحرفي.
- #145 إزالة إعادة قراءة آخر نافذة (كانت بتعمل تكرار).
- #144 شفاء ذاتي (يرجّع VoiceX لما النفق يرجع).
- #143 نسخ الكل بالتفاصيل + إصلاح الحي.
- #141 مشاركة كل لوحات الصوت على واتساب (نص مختصر + نسخ الكل).
- #139 ألوان أزرار الصلاحيات (أخضر مفتوح/أحمر مقفول).
- #138 وسم الحصاد باسم المندوب (X-Agent-Id).
- #137 الأدمن يتحكّم في صوت المناديب. #136 زر مشاركة إكسيل.
- #132–#134 محرك المعمل بالحرف + مؤشّر صوت + آخر لوحة ماتضيعش.

## قرار السيرفر المؤجّر (جاري)
- الموديل صغير (~١ جيجا) → كرت ١٦ جيجا كفاية. **A4000 مخصّص (~$125/شهر, RunPod)** موصى به (لينك ثابت = ينهي مشكلة النفق).
- التكلفة الكلية ~$171/شهر (سيرفر + Vercel $20 + Supabase $25 + دومين). حاسبة التعادل اتعملت كـArtifact.
- **مستني:** المالك يحدّد سعر الاشتراك + عدد المشتركين المتوقّع عشان نحسب هل يجازي.

## قواعد شغل مهمة (من الذاكرة)
- **ترفرف اللوحة:** المالك رفض الدمج بالثقة (خوف يسيب الغلط). الحل المقبول = حارس توأم بـ**اتفاق النوافذ** (يشيل المفرد بس لو ليه توأم مؤكّد نافذتين+، عمره ما يرمي مؤكّدة → الأسطول الحقيقي يفضل).
- **الحل الجذري للترفرف/الـonset = تدريب** على الحصاد (مش كود).
- كل fix فرع نضيف من origin/main (الريبو squash-merge فبيباعد).
- عربي أول في الردود.

## الخطوة الجاية المقترحة
1. تثبيت السيرفر المؤجّر (ينهي مشاكل النفق).
2. تجهيز التدريب على الحصاد (تنضيف pseudo-labels + holdout + وازن الحرف الأول).
