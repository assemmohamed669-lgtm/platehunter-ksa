-- ═══════════════════════════════════════════════════════════════════════════
-- إضافة عمود «عليه» (المتبقّي على المندوب) لصفحة حسابات المناديب.
-- شغّل الملف ده مرة واحدة على Supabase (SQL editor) — بعد agent-payments.sql.
-- «دفع» بيتخزّن في جدول agent_payments زي ما هو؛ «عليه» رقم واحد على المندوب.
-- ═══════════════════════════════════════════════════════════════════════════

alter table public.profiles add column if not exists owed_amount numeric;
