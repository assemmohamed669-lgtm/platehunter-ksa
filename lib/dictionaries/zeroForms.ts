/**
 * صيغ الصفر المعرَّبة — بيانات نقية منقولة حرفياً من `plateParser.ts`
 * ==================================================================
 * نقل حرفي لـ `ZERO_WORD_RE`. بيمسك عائلة زير/زيرو/زيرة/زيره/زيرا/زيرى
 * (arabized "zero") ككلمة مستقلة فقط — الـ lookaround بيمنع مسكها جوّه
 * كلمة عربية تانية (زي «وزير»). «صفر» نفسها في `numbers.ts`.
 */
export const ZERO_WORD_RE = /(?<![؀-ۿ])زير[وةهاى]?(?![؀-ۿ])/g;
