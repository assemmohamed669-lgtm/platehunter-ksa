/**
 * ══════════════════════════════════════════════════════════════════════
 *  🙈 إخفاء «صوتي» — Voice PRO بقت البديل
 * ══════════════════════════════════════════════════════════════════════
 *
 * المالك (٢٤ سبتمبر ٢٠٢٦): «هنخفي صفحة صوتي اللي في التشييك علشان هي على سيرفر
 * ومالهاش لازمة دلوقتي بعد ما نزّلنا بديل أفضل للصوت… ابدأ اخفي صفحة صوتي».
 *
 * «صوتي» بتظهر في مكانين: تاب جوّه صفحة التشييك، وزرار في الشريط التحتي للمشترك
 * «صوت بس». الاتنين بياخدوا القرار من هنا.
 *
 * 🔒 **السوبر أدمن الأول** (قاعدة المالك: «متنشرش التعديل غير للسوبر أدمن»). الفتح
 * للكل = `sawtiHiddenFor` ترجّع `true` على طول (سطر واحد).
 *
 * 🔴 **مفيش شغل يضيع**: لوحات «صوتي» بتتشال منها بعد ما تتصدّر، فلو لسه فيها لوحات
 * يبقى فيه شغل ماتصدّرش ⇒ التاب بيفضل ظاهر لحد ما المندوب يصدّرها أو يمسحها.
 */
export function sawtiHiddenFor(_isSuper?: boolean): boolean {
  // 🙈 للكل — المالك (٢٤ سبتمبر ٢٠٢٦): «اقفل على المناديب بقى صوتي» (وسيرفر كوريا اتقفل).
  void _isSuper;
  return true;
}

export function sawtiTabVisible(o: { hidden: boolean; leftover: number }): boolean {
  if (!o.hidden) return true;
  return o.leftover > 0;
}
