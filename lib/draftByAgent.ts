/**
 * ══════════════════════════════════════════════════════════════════════
 *  🔴 لوحات كل مندوب لاسمه — مسودّة Voice PRO على موبايل مشترك
 * ══════════════════════════════════════════════════════════════════════
 *
 * المالك (٢٤ سبتمبر ٢٠٢٦): «اللوحات بتاع كل مندوب تروح لاسمه… ده شغل مناديب وتعبهم،
 * مش عايز فيه أي غلط حتى لو صغير».
 *
 * المسودّة (`checkDrafts` · مفتاح `trial`) واحدة للموبايل كله، واللوحات **مابتتمسحش**
 * حتى بعد تسجيل الخروج (قاعدة المالك). فمندوب تاني يدخل على نفس الموبايل كان بيشوف
 * لوحات الأول — ولو صدّرها **بتتسجّل باسمه هو**.
 *
 * ⇒ كل لوحة بتتختم بحساب المندوب لحظة ما تظهر (`agentId`). لوحات حساب تاني بتتخبّى
 *   **ومابتتمسحش** (بتفضل في المسودّة لحد ما صاحبها يدخل)، والتصدير بيرفضها.
 *   اللوحات القديمة من غير ختم (قبل التعديل ده) بتفضل زي ما كانت.
 */
export interface AgentStamped {
  agentId?: string | null;
}

/** اللوحة دي ينفع تتصدّر/تظهر للحساب ده؟ — لوحته هو، أو من غير ختم (قديمة). */
export function isMine(row: AgentStamped, uid: string | null | undefined): boolean {
  if (!uid) return true;
  return !row.agentId || row.agentId === uid;
}

/** بيقسم الصفوف: اللي تظهر للحساب ده، واللي تفضل مستخبية في المسودّة لأصحابها. */
export function splitByAgent<T extends AgentStamped>(
  rows: readonly T[],
  uid: string | null | undefined,
): { mine: T[]; others: T[] } {
  const mine: T[] = [];
  const others: T[] = [];
  for (const row of rows ?? []) (isMine(row, uid) ? mine : others).push(row);
  return { mine, others };
}
